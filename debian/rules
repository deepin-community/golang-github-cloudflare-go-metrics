#!/usr/bin/make -f

%:
	dh $@ --buildsystem=golang --with=golang

override_dh_auto_test:
	# TODO: fix failing tests
	-dh_auto_test

override_dh_auto_install:
	dh_auto_install
	rm -r debian/golang-github-cloudflare-go-metrics-dev/usr/bin
